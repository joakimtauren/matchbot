{
  "name": "slack-matcher-bot",
  "version": "1.0.0",
  "description": "Slack bot for matching users within channels using PostgreSQL",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon app.js",
    "test": "jest",
    "setup": "node setup.js",
    "db:migrate": "sequelize-cli db:migrate"
  },
  "dependencies": {
    "@slack/bolt": "^3.12.2",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "pg": "^8.9.0",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.28.0",
    "winston": "^3.8.2"
  },
  "devDependencies": {
    "jest": "^29.4.3",
    "nodemon": "^2.0.20",
    "sequelize-cli": "^6.6.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
